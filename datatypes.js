var global1;
let gloabl2; // variables declared with let are bound to block scopes and can't be redeclared within the same block
const global3; // behave like varibles declared with let but can't be reassigned

var n; //undefined 
var n = 0; // number
var b = true; // boolean
var s = "a" //string
var o = {key1 : "value_to_key1", key2: "value_to_key2"} // object

var a = ["a", "b", "c"]; //array
var firstvalueofarray1 = array1[0]; //string